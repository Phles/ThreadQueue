# Compile with G++
buildLinux:
    tags:
        - linux
        - console
    stage: build
    script:
        - g++ -std=c++17 ./ThreadQueue/test.cpp -o test.out
    artifacts:
        paths:
            - test.out